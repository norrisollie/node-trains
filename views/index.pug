<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Next departure from !{origin}

    body
        h1 Next departure from !{origin}

        #wrapper
            #std-destination !{std} to !{destination} !{via}
            
